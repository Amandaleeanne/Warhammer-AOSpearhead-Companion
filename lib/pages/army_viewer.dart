import 'package:flutter/material.dart';

class ArmyViewer extends StatelessWidget {
  const ArmyViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Yay! almost done!"),
    );
  }
}